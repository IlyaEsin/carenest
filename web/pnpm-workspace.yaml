packages:
  - apps/*
  - packages/*

# pnpm 10 skips dependency build scripts unless listed; esbuild and sharp ship native binaries,
# msw's postinstall sets up its browser worker script.
onlyBuiltDependencies:
  - esbuild
  - msw
  - sharp
